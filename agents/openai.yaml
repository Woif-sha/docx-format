interface:
  display_name: "中文论文 Word 排版"
  short_description: "中文论文 DOCX 默认排版与校验规范"
  default_prompt: "Use $chinese-paper-docx-format to format this Chinese academic DOCX with the user default paper rules."

policy:
  allow_implicit_invocation: true
