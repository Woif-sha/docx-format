interface:
  display_name: "DOCX 中文论文排版"
  short_description: "DOCX 默认排版、三线表与中文引号校验"
  default_prompt: "Use $docx-format to format this Chinese academic DOCX with the user default paper rules."

policy:
  allow_implicit_invocation: true
